# Identicon

ユーザー名をもとに、GitHub の Identicon のような左右対称のアイコンを生成する Java プログラムです。

Java の学習を兼ねて、ハッシュ値・ビット演算・2次元配列・画像生成などを使って実装しています。

> 🚧 **現在リファクタリング中です。**  
> コード構成や命名、処理の分割などは今後変更する予定です。

## Overview

名字と名前を入力すると、それらを結合した文字列からハッシュ値を生成し、その値を使って 7×7 の Identicon 風アイコンを作成します。

現在は、同じハッシュ値から配置方法の異なる2種類のアイコンを生成します。

生成例：

```text
iconA.png
iconB.png
```

## How it works

処理の大まかな流れは以下の通りです。

```text
名字 + 名前
    ↓
文字列を結合
    ↓
hashCode()
    ↓
32bit のハッシュ値
    ↓
┌──────────────┬──────────┐
│ 28bit        │ 4bit     │
│ icon.        │ color.   │
└──────────────┴──────────┘
    ↓
7×7 の左右対称パターンを生成
    ↓
PNGとして出力
```

### Input

現在は名字と名前をそれぞれ標準入力から受け取ります。

例：

```text
YAMADA
TARO
```

内部では以下のような形式に変換して使用します。

```text
YAMADA:TARO
```

> 📝 **ただいま編集中**  
> 入力仕様や入力チェックについては、今後整理・追加する予定です。

## Pattern generation

Java の `String#hashCode()` から得られる 32bit の値を利用しています。

- 上位28bit：アイコンの配置
- 下位4bit：色の選択

7×7 のアイコンを左右対称にするため、実際にハッシュ値から決定するのは片側3列と中央1列の、合計28マスです。

```text
A B C D C B A
```

現在は、28bit の配置方法を変えた2種類のパターンを生成しています。

> 📝 **ただいま編集中**  
> `iconA` と `iconB` のどちらを最終仕様として採用するか、または両方残すか検討中です。

## Color

ハッシュ値の下位4bitを利用し、0〜15の値を取得します。

その値を16色の固定カラーパレットに対応させて、Identicon の色を決定しています。

```text
4bit
↓
0 ～ 15
↓
16色のカラーパレットから1色選択
```

> 📝 **ただいま編集中**  
> カラーパレットの内容や色の決定方法については、今後変更する可能性があります。

## Output

生成された画像は PNG 形式で保存されます。

```text
iconA.png
iconB.png
```

現在の画像サイズは以下の通りです。

- アイコン本体：7×7セル
- 外側余白：上下左右1セル
- 全体：9×9セル
- 1セル：40px
- 画像サイズ：360×360px

## Technologies

- Java
- Maven
- `java.awt.Graphics2D`
- `java.awt.image.BufferedImage`
- `javax.imageio.ImageIO`

> 📝 **ただいま編集中**  
> Java・Mavenのバージョンや実行環境については、確認後に記載予定です。

## How to run

> 📝 **ただいま編集中**  
> 実行方法は、プロジェクト構成の整理後に記載予定です。

予定している内容：

```text
1. Repositoryをclone
2. Java / Mavenの環境を準備
3. プログラムを実行
4. 名字と名前を入力
5. PNGファイルを生成
```
### Headless環境での実行

このプログラムでは、PNG画像を生成するために `BufferedImage` や `Graphics2D` などの `java.awt` 関連クラスを使用しています。

GUI環境のないLinuxサーバーやコンテナ環境などでは、AWTの初期化時にエラーが発生する場合があります。

その場合は、JavaをHeadlessモードで実行してください。

```bash
java -Djava.awt.headless=true -jar <jarファイル名>.jar
```

このプログラムではウィンドウなどのGUI表示は行っておらず、AWTはメモリ上でIdenticonを描画し、PNGファイルとして保存するためにのみ使用しています。

> 📝 **補足**
>
> WindowsやmacOSなど、通常のデスクトップ環境ではこのオプションが不要な場合があります。  
> 画面表示環境のない実行環境でAWT関連のエラーが発生した場合に使用してください。

## Project structure

> 📝 **ただいま編集中**  
> リファクタリングに伴い、クラス構成やディレクトリ構成を変更する可能性があります。

## Refactoring

現在、最初に動作するところまで実装したコードをもとにリファクタリングを進めています。

主に以下を整理する予定です。

- `main` メソッドに集中している処理の分割
- Identicon生成処理のメソッド化
- PNG出力処理の共通化
- 変数名の整理
- マジックナンバーの整理
- デバッグ用出力の削除
- 入力処理の整理
- 必要に応じたクラス分割

> 🚧 **現在リファクタリング中のため、コード構成は変更される可能性があります。**

## Purpose

このプロジェクトは、本格的な Identicon ライブラリを作ることを目的としたものではありません。

Java の学習・個人開発の練習として、以下の内容を実際に使って理解することを目的にしています。

- ハッシュ値
- 2進数
- ビット演算
- `&`
- `>>>`
- 2次元配列
- 左右対称なデータ生成
- `BufferedImage`
- `Graphics2D`
- PNGファイル出力
- Git / GitHub
- Maven

> 📝 **ただいま編集中**  
> このプロジェクトを作って学んだことや、実装で工夫した点は後から追記予定です。

## License

> 📝 **ただいま編集中**  
> ライセンスについては現在検討中です。